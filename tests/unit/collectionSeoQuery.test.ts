import { describe, it } from 'vitest';

describe('COLLECTION_SEO_QUERY', () => {
  it.todo('is exported from @shared/sanity/lib/query');
  it.todo('filters by surface, collectionHandle, gender (gender ignored when surface=brand)');
  it.todo('projects post-> with translations via translation.metadata');
  it.todo('returns null when no matching collectionSeo doc exists');
});
